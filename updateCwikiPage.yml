---
stub: "https://cwiki.apache.org/confluence/rest/api"
headers:
  Content-Type: "application/json"
auth:
  username:
  password:
tasks:
  - name: "search for content with label"
    uri: "content/search"
    req: "get"
    data:
      cql: "space=Infrastructure"
    output:
      type: "print"
      show: "headers"
verbose: True
